
<h1>Eat-Da-Burger!</h1>
<img src="../public/assets/img/buger8bit.png" alt="Girl in a jacket" width="500" height="600">
<div class="row">
  <div class="col-6">
<h2>Burgers devoured! </h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>
</div>
<div class="col-6">
<h2>Burgers waiting to be devoured!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>
</div>
</div>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="bu">Burger Name:</label>
    <input type="text" id="bu" name="name">
  </div>
    <div class="form-group">
    <label for="devoured">Devoured?</label><br>
    <input type="radio" name="devoured" value="1"> Devoured!<br>
    <input type="radio" name="devoured" value="0"> Waiting to be devoured!
  </div>

  <button type="submit">Add burger</button>
</form>